<div class="row">
  <div class="col-md-6 col-md-offset-3">

<%= form_for(@energy_level) do |f| %>
  <% if @energy_level.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@energy_level.errors.count, "error") %> prohibited this energy_level from being saved:</h2>

      <ul>
      <% @energy_level.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <div class="field">
    <%= f.label :energy_level %><br>
    <%= f.number_field :energy_level, :autofocus => "autofocus", :placeholder => " 1-4" %>
  </div>

        <br/>
        <br/>

  <div class="actions">
    <%= f.submit "Update Current Energy Level", class: "btn btn-primary" %>
  </div>
<% end %>

  </div>
</div>