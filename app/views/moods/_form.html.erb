<div class="row">
  <div class="col-md-6 col-md-offset-3">

<%= form_for(@mood) do |f| %>
  <% if @mood.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@mood.errors.count, "error") %> prohibited this mood from being saved:</h2>

      <ul>
      <% @mood.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <div class="field">
    <%= f.label :mood %><br>
    <%= f.number_field :mood, :autofocus => "autofocus", :placeholder => " 1-7" %>
  </div>

        <br/>
        <br/>

  <div class="actions">
    <%= f.submit "Add Mood Status", class: "btn btn-primary" %>
  </div>
<% end %>

  </div>
</div>