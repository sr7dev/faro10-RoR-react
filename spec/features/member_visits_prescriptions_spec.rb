require "rails_helper"

feature "user visits prescriptions page" do
  scenario "from dashboard" do

    # prescriptions links commented out on dashboard

    # member = create(:member)

    # login_as(member, :scope => :user)
    # visit dashboard_path(as: member)

    # click_link('main_prescriptions')

    # expect(page).to have_current_path(prescriptions_path)
    # expect(page).to have_content "Prescription Consistency"
    
  end
end
